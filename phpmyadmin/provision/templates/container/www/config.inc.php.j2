<?php
// container_managed=true                                                     //
//                                                                            //
// NOTE: by default this file is automatically generated on container startup.//
// To disable the autogeneration simply change the first line of this file to //
// 'container_managed=false'                                                  //
////////////////////////////////////////////////////////////////////////////////


/**
 * phpMyAdmin sample configuration, you can use it as base for
 * manual configuration. For easier setup you can use setup/
 *
 * All directives are explained in documentation in the doc/ folder
 * or at <https://docs.phpmyadmin.net/>.
 *
 * @package PhpMyAdmin
 */

/**
 * This is needed for cookie based authentication to encrypt password in
 * cookie. Needs to be 32 chars long.
 */
$cfg['blowfish_secret'] = '{{ PHPMYADMIN_BLOWFISH_SECRET }}';

/**
 * Servers configuration
 */
$i = 0;

/**
 * First server
 */
$i++;
/* Authentication type */
$cfg['Servers'][$i]['auth_type'] = '{{ PHPMYADMIN_AUTH_TYPE }}';
/* Server parameters */
$cfg['Servers'][$i]['host'] = '{{ PHPMYADMIN_HOST }}';
$cfg['Servers'][$i]['socket'] = '{{ PHPMYADMIN_SOCKET }}';
$cfg['Servers'][$i]['compress'] = {{ PHPMYADMIN_COMPRESS }};
$cfg['Servers'][$i]['AllowNoPassword'] = {{ PHPMYADMIN_ALLOWNOPASSWORD }};

/**
 * phpMyAdmin configuration storage settings.
 */

/* User used to manipulate with storage */
// $cfg['Servers'][$i]['controlhost'] = '';
// $cfg['Servers'][$i]['controlport'] = '';
// $cfg['Servers'][$i]['controluser'] = 'pma';
// $cfg['Servers'][$i]['controlpass'] = 'pmapass';

/* Storage database and tables */
{{ '//' if not (PHPMYADMIN_ENABLE_BOOKMARK or PHPMYADMIN_ENABLE_RELATION or PHPMYADMIN_ENABLE_TABLE_INFO or PHPMYADMIN_ENABLE_TABLE_COORDS or PHPMYADMIN_ENABLE_PDF_PAGES or PHPMYADMIN_ENABLE_COLUMN_INFO or PHPMYADMIN_ENABLE_HISTORY or PHPMYADMIN_ENABLE_TABLE_UIPREFS or PHPMYADMIN_ENABLE_TRACKING or PHPMYADMIN_ENABLE_USERCONFIG or PHPMYADMIN_ENABLE_RECENT or PHPMYADMIN_ENABLE_FAVORITE or PHPMYADMIN_ENABLE_USERS or PHPMYADMIN_ENABLE_USERGROUPS or PHPMYADMIN_ENABLE_NAVIGATIONHIDING or PHPMYADMIN_ENABLE_SAVEDSEARCHES or PHPMYADMIN_ENABLE_CENTRAL_COLUMNS or PHPMYADMIN_ENABLE_DESIGNER_SETTINGS or PHPMYADMIN_ENABLE_EXPORT_TEMPLATES) else '' }}$cfg['Servers'][$i]['pmadb'] = 'phpmyadmin';
{{ '//' if not PHPMYADMIN_ENABLE_BOOKMARK }}$cfg['Servers'][$i]['bookmarktable'] = 'pma__bookmark';
{{ '//' if not PHPMYADMIN_ENABLE_RELATION }}$cfg['Servers'][$i]['relation'] = 'pma__relation';
{{ '//' if not PHPMYADMIN_ENABLE_TABLE_INFO }}$cfg['Servers'][$i]['table_info'] = 'pma__table_info';
{{ '//' if not PHPMYADMIN_ENABLE_TABLE_COORDS }}$cfg['Servers'][$i]['table_coords'] = 'pma__table_coords';
{{ '//' if not PHPMYADMIN_ENABLE_PDF_PAGES }}$cfg['Servers'][$i]['pdf_pages'] = 'pma__pdf_pages';
{{ '//' if not PHPMYADMIN_ENABLE_COLUMN_INFO }}$cfg['Servers'][$i]['column_info'] = 'pma__column_info';
{{ '//' if not PHPMYADMIN_ENABLE_HISTORY }}$cfg['Servers'][$i]['history'] = 'pma__history';
{{ '//' if not PHPMYADMIN_ENABLE_TABLE_UIPREFS }}$cfg['Servers'][$i]['table_uiprefs'] = 'pma__table_uiprefs';
{{ '//' if not PHPMYADMIN_ENABLE_TRACKING }}$cfg['Servers'][$i]['tracking'] = 'pma__tracking';
{{ '//' if not PHPMYADMIN_ENABLE_USERCONFIG }}$cfg['Servers'][$i]['userconfig'] = 'pma__userconfig';
{{ '//' if not PHPMYADMIN_ENABLE_RECENT }}$cfg['Servers'][$i]['recent'] = 'pma__recent';
{{ '//' if not PHPMYADMIN_ENABLE_FAVORITE }}$cfg['Servers'][$i]['favorite'] = 'pma__favorite';
{{ '//' if not PHPMYADMIN_ENABLE_USERS }}$cfg['Servers'][$i]['users'] = 'pma__users';
{{ '//' if not PHPMYADMIN_ENABLE_USERGROUPS }}$cfg['Servers'][$i]['usergroups'] = 'pma__usergroups';
{{ '//' if not PHPMYADMIN_ENABLE_NAVIGATIONHIDING }}$cfg['Servers'][$i]['navigationhiding'] = 'pma__navigationhiding';
{{ '//' if not PHPMYADMIN_ENABLE_SAVEDSEARCHES }}$cfg['Servers'][$i]['savedsearches'] = 'pma__savedsearches';
{{ '//' if not PHPMYADMIN_ENABLE_CENTRAL_COLUMNS }}$cfg['Servers'][$i]['central_columns'] = 'pma__central_columns';
{{ '//' if not PHPMYADMIN_ENABLE_DESIGNER_SETTINGS }}$cfg['Servers'][$i]['designer_settings'] = 'pma__designer_settings';
{{ '//' if not PHPMYADMIN_ENABLE_EXPORT_TEMPLATES }}$cfg['Servers'][$i]['export_templates'] = 'pma__export_templates';

/**
 * End of servers configuration
 */

/**
 * Directories for saving/loading files from server
 */
$cfg['UploadDir'] = '{{ PHPMYADMIN_UPLOADDIR }}';
$cfg['SaveDir'] = '{{ PHPMYADMIN_SAVEDIR }}';

/**
 * Defines the default connection collation to use, if not user-defined.
 */
$cfg['DefaultConnectionCollation'] = 'utf8_unicode_ci';

/**
 * Whether to display icons or text or both icons and text in table row
 * action segment. Value can be either of 'icons', 'text' or 'both'.
 * default = 'both'
 */
$cfg['RowActionType'] = '{{ PHPMYADMIN_ROWACTIONTYPE }}';

/**
 * Defines whether a user should be displayed a "show all (records)"
 * button in browse mode or not.
 * default = false
 */
$cfg['ShowAll'] = {{ PHPMYADMIN_SHOWALL }};

/**
 * Number of rows displayed when browsing a result set. If the result
 * set contains more rows, "Previous" and "Next".
 * Possible values: 25, 50, 100, 250, 500
 * default = 25
 */
$cfg['MaxRows'] = {{ PHPMYADMIN_MAXROWS }};

/**
 * Disallow editing of binary fields
 * valid values are:
 *   false    allow editing
 *   'blob'   allow editing except for BLOB fields
 *   'noblob' disallow editing except for BLOB fields
 *   'all'    disallow editing
 * default = 'blob'
 */
$cfg['ProtectBinary'] = {{ PHPMYADMIN_PROTECTBINARY }};

/**
 * Default language to use, if not browser-defined or user-defined
 * (you find all languages in the locale folder)
 * uncomment the desired line:
 * default = 'en'
 */
$cfg['DefaultLang'] = '{{ PHPMYADMIN_DEFAULTLANG }}';

/**
 * How many columns should be used for table display of a database?
 * (a value larger than 1 results in some information being hidden)
 * default = 1
 */
$cfg['PropertiesNumColumns'] = {{ PHPMYADMIN_PROPERTIESNUMCOLUMNS }};

/**
 * Set to true if you want DB-based query history.If false, this utilizes
 * JS-routines to display query history (lost by window close)
 *
 * This requires configuration storage enabled, see above.
 * default = false
 */
$cfg['QueryHistoryDB'] = {{ PHPMYADMIN_QUERYHISTORYDB }};

/**
 * When using DB-based query history, how many entries should be kept?
 * default = 25
 */
$cfg['QueryHistoryMax'] = {{ PHPMYADMIN_QUERYHISTORYMAX }};

/**
 * Whether or not to query the user before sending the error report to
 * the phpMyAdmin team when a JavaScript error occurs
 *
 * Available options
 * ('ask' | 'always' | 'never')
 * default = 'ask'
 */
$cfg['SendErrorReports'] = '{{ PHPMYADMIN_SENDERRORREPORTS }}';

/**
 * You can find more configuration options in the documentation
 * in the doc/ folder or at <https://docs.phpmyadmin.net/>.
 */

$cfg['VersionCheck'] = false;
