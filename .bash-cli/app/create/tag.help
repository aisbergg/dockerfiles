
Create a Git tag using the release information of the image.

  -h, --help            Display help message and quit
  -f, --force           Create a tag even though the tag already exists and the working directory is not clean
